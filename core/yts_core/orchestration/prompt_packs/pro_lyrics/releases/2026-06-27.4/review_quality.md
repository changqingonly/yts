请评估以下歌词是否适合进入 Suno 音频生成队列。
必须检查：是否严格执行 Pro Hook、是否满足 selected_blueprint 结构、是否避开用户否定项、是否有完整情绪弧、是否有可唱性、是否包含会被 Suno 误唱的中文 meta 文本。
decision 只能是 pass、repair 或 block；不要虚构通过。
输出 JSON 字段只能包含：decision、bucket、submit_suno、safety、overall_score、main_issues、suggestions、rationale。
